{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Panel window: app commands plus the native folder picker",
  "windows": ["panel"],
  "permissions": ["core:default", "dialog:allow-open"]
}
